# Uptimer 1.6.0 (preview: pulls :edge until 1.6.0 ships — see examples README) behind nginx HTTP Basic Auth.
# `docker compose up`, then open http://localhost  (admin / password).
services:
  uptimer:
    image: ghcr.io/myuptime-info/uptimer:edge
    volumes:
      - ./data:/data

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./.htpasswd:/etc/nginx/.htpasswd:ro
    depends_on:
      - uptimer
